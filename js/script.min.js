/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ([
/* 0 */,
/* 1 */
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   isWebp: () => (/* binding */ isWebp)
/* harmony export */ });


    
 function isWebp() {
        function testWebP(callback) {
            let webP = new Image;
            webP.onload = webP.onerror = function() {
                callback(webP.height == 2);
            };
            webP.src = "data:image/webp;base64,UklGRjoAAABXRUJQVlA4IC4AAACyAgCdASoCAAIALmk0mk0iIiIiIgBoSygABc6WWgAA/veff/0PP8bA//LwYAAA";
        }
        testWebP((function(support) {
            let className = support === true ? "webp" : "no-webp";
            document.documentElement.classList.add(className);
        }));
    }
    


/***/ })
/******/ 	]);
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other modules in the chunk.
(() => {
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _modules_functions_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(1);

_modules_functions_js__WEBPACK_IMPORTED_MODULE_0__.isWebp()

let body = document.querySelector('body')

function load() {
    body.style.opacity = '0'

    window.addEventListener('load', (e) => {
        body.style.opacity = '1'

        function burgerActive() {
            let burger = document.querySelector('.burger')
            let nav = document.querySelector('.head__nav')

            burger.addEventListener('click', (e) => {
                burger.classList.toggle('active')

                nav.classList.toggle('active')
                body.classList.toggle('active')
            })
        }
        burgerActive()

        function dinamicSlide() {
            let lines = document.querySelectorAll('.creat__line.line')
            let blocks = document.querySelectorAll('.creat__img')
            let btnSlideLeft = document.querySelector('.btnslides__left')
            let btnSlideRight = document.querySelector('.btnslides__right')
            let timer = 9 // css .test__line.active {transition-duration: timer + 1}
            let timerCopy = 9
            let indLine = 0
            let timeTimer = 1000
            let timeOk = true

            lines[0].classList.add('active')
            blocks[0].classList.add('active')


            let myInterval = setInterval(() => {
                timer--
                btnSlideLeft.addEventListener('mouseove', (e) => {
                    clearInterval(myInterval)

                })
                btnSlideRight.addEventListener('mouseove', (e) => {
                    clearInterval(myInterval)

                })
                btnSlideLeft.addEventListener('click', (e) => {
                    if (timeOk) {
                        timer = timerCopy
                        indLine--

                        if (indLine < 0) {
                            indLine = lines.length - 1

                        }
                        vewLine(indLine)

                        timeOk = false
                        setTimeout(() => {
                            timeOk = true
                        }, timeTimer)
                    }

                })
                btnSlideRight.addEventListener('click', (e) => {


                    if (timeOk) {
                        timer = timerCopy
                        indLine++

                        if (indLine >= lines.length) {
                            indLine = 0
                        }
                        vewLine(indLine)

                        timeOk = false
                        setTimeout(() => {
                            timeOk = true
                        }, timeTimer)
                    }

                })


                function vewLine(ind) {
                    lines.forEach((line) => {
                        line.classList.remove('active')
                    })
                    blocks.forEach((block) => {
                        block.classList.remove('active')
                    })
                    lines[ind].classList.add('active')

                    blocks[ind].classList.add('active')

                }


                lines.forEach((line, index) => {
                    line.addEventListener('click', (e) => {

                        if (!line.classList.contains('active')) {
                            timer = timerCopy
                            indLine = index
                            vewLine(index)

                        }

                    })

                })

                if (timer < 0) {
                    timer = timerCopy
                    indLine++

                    if (indLine >= lines.length) {
                        indLine = 0
                    }

                    vewLine(indLine)
                }


            }, 1000)



        }
        dinamicSlide()

        function videoPlay() {
            let video = document.querySelector('.video__video')
            let videoButn = document.querySelector('.video__butn')
            let vidoMoot = document.querySelector('.video__music')
            let videCont = document.querySelector('.video__videocont')
            let videoImg = document.querySelector('.video__img')
            let videoStatus = 'pause'
            videCont.addEventListener('click', (e) => {

                if (e.target.closest('.video__butn') && videoStatus === 'pause') {
                    videoStatus = 'play'
                    video.play()
                    video.classList.add('active')
                    videoImg.classList.add('non')
                    videoButn.classList.add('non')

                }
                if (!e.target.closest('.video__butn') && videoStatus === 'play') {
                    videoStatus = 'pause'
                    video.pause()
                    video.classList.remove('active')
                    videoImg.classList.remove('non')
                    videoButn.classList.remove('non')
                    console.log('ok');
                }

            })


        }
        videoPlay()

        function serviesItems() {
            let items = document.querySelectorAll('.item')
            items.forEach((item) => {
                item.addEventListener('mouseover', (e) => {
                    item.classList.add('active')
                })
                item.addEventListener('mouseout', (e) => {
                    item.classList.remove('active')
                })
            })
        }
        serviesItems()

        function swiperSlider() {
            var swiper = new Swiper(".slide__slider", {
                slidesPerView: 3,
                spaceBetween: 30,
                loop: true,

                navigation: {
                    nextEl: ".swiper-button-next",
                    prevEl: ".swiper-button-prev",
                },
                breakpoints: {
                    0: {
                        slidesPerView: 1,

                    },
                    420: {
                        slidesPerView: 2,

                    },
                    820: {
                        slidesPerView: 3,

                    },
                    1920: {
                        slidesPerView: 3,

                    },

                },
            });

            let swiperItemBody = document.querySelectorAll('.item__body')

            swiperItemBody.forEach((item) => {
                item.addEventListener('mouseover', (e) => {
                    item.classList.add('active')
                })
                item.addEventListener('mouseout', (e) => {
                    item.classList.remove('active')
                })

            })
            let swiperBtnSlides = document.querySelectorAll('.btnslide')
            function vewSwiperSlide(ind) {
                swiperBtnSlides.forEach((btn) => {
                    btn.classList.remove('active')
                })
                swiperBtnSlides[ind].classList.add('active')
            }
            swiperBtnSlides.forEach((btn, index) => {
                btn.addEventListener('click', (e) => {
                    vewSwiperSlide(index)
                })
            })
        }
        swiperSlider()

        function dinamicSlide2() {
            let arrowLeft = document.querySelector('.comm__arrowleft')
            let arrowRight = document.querySelector('.comm__arrowright')

            let imgs = document.querySelectorAll('.comm__img img')
            let items = document.querySelectorAll('.comm__item')

            let index = 0
            let status = true
            arrowLeft.addEventListener('click', (e) => {
                if (status) {
                    index--

                    if (index < 0) {
                        index = imgs.length - 1
                    }
                    vewSection(index)
                    status = false
                    setTimeout(() => {
                        status = true
                    }, 1000)
                }
            })
            arrowRight.addEventListener('click', (e) => {
                if (status) {
                    index++

                    if (index >= imgs.length) {
                        index = 0
                    }
                    vewSection(index)
                    status = false
                    setTimeout(() => {
                        status = true
                    }, 1000)
                }
            })


            function vewSection(index) {
                imgs.forEach((img) => {
                    img.classList.remove('active')

                })
                items.forEach((item) => {
                    item.classList.remove('active')
                })
                imgs[index].classList.add('active')
                items[index].classList.add('active')

            }
        }
        dinamicSlide2()

        
        function animationItm() {
            let itms = document.querySelectorAll('.itm')


            itms.forEach((itm) => {
                let newsMinTitle = itm.querySelector('.news__mintitle')
                let newsImg = itm.querySelector('.news__img')
                itm.addEventListener('mouseover', (e) => {
                    newsMinTitle.classList.add('active')
                    newsImg.classList.add('active')
                })
                itm.addEventListener('mouseout', (e) => {
                    newsMinTitle.classList.remove('active')
                    newsImg.classList.remove('active')
                })
            })
        }
        animationItm()

        function btnUp() {
            let btnUp = document.querySelector('.down__upbtn')

            btnUp.addEventListener('mouseover', (e) => {
                btnUp.classList.add('active')
            })
            btnUp.addEventListener('mouseout', (e) => {
                btnUp.classList.remove('active')
            })

            btnUp.addEventListener('click', (e) => {
                window.scrollTo({
                    top: 0,
                    left: 0,
                    behavior: 'smooth'
                })
            })
        }
        btnUp()
      async function scrollAnimation() {
            

            
          await function callback(entries) {
                entries.forEach((entry) => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('active')

                    }

                })

            }
            progressBar()
            let options = {
                threshold: .3,

            }
            let observer = new IntersectionObserver(callback, options)

            let title = document.querySelectorAll('._title')
            let text = document.querySelectorAll('._text')
            let video = document.querySelector('.video__videocont')
            let value = document.querySelectorAll('.card__img')
            let cardTitle = document.querySelectorAll('.card__title')
            let cardText = document.querySelectorAll('.card__text')
            let message = document.querySelector('.message')
            let commImg = document.querySelector('.comm__img img')
            let newRow = document.querySelector('.news__row')
            let serRow = document.querySelector('.servies__row')
            let form = document.querySelector('.form__form')
            let slide = document.querySelector('.slide__slider')
            let suprtRow = document.querySelector('.suprt__row')
            let progres = document.querySelectorAll('.fact__progress')
            observer.observe(suprtRow)
            observer.observe(slide)
            observer.observe(form)
            observer.observe(serRow)
            observer.observe(newRow)
            observer.observe(commImg)
            observer.observe(message)
            observer.observe(video)
            title.forEach((tit) => {
                observer.observe(tit)
            })
            text.forEach((tex) => {
                observer.observe(tex)
            })
            value.forEach((val) => {
                observer.observe(val)
            })
            cardTitle.forEach((card) => {
                observer.observe(card)
            })
            cardText.forEach((ct) => {
                observer.observe(ct)
            })
            progres.forEach((pro) => {
                observer.observe(pro)

            })



        }
        scrollAnimation()

        function progressBar() {

           
           
            
            let progressValue = document.querySelectorAll('.fact__value')
            let start
            let end
            let step
            let time = 100

            let startBc
            let endBc
            let timeBc = 10


            progressValue.forEach((value) => {
                let parent =  value.parentNode

                if (parent.classList.contains('active')) {
                    console.log('yes');
                    if (parent.classList.contains('orange')) {
                        start = 0
                        end = 98
                        step = 1

                        let interval = setInterval(() => {
                            start += step

                            value.textContent = `${start}%`
                            if (start === end) {
                                clearInterval(interval)
                            }
                        }, 35)
                        startBc = 0
                        endBc = 300

                        let intervalBc = setInterval(() => {
                            startBc++
                            parent.style.background = `conic-gradient(#ff9500 ${startBc}deg, #ffc97d 0deg)`
                            if (startBc === endBc) {
                                clearInterval(intervalBc)
                            }
                        }, timeBc)

                    }
                    if (parent.classList.contains('red')) {
                        let start = 0
                        let end = 20
                        let step = 1

                        let interval2 = setInterval(() => {
                            start += step

                            value.textContent = `${start}`
                            if (start === end) {
                                clearInterval(interval2)
                            }
                        }, 80)
                        let startBc = 0
                        let endBc = 270
                        let timeBc = 5

                        let intervalBc2 = setInterval(() => {
                            startBc++
                            parent.style.background = `conic-gradient(#ff0000 ${startBc}deg, #ff9090 0deg)`
                            if (startBc === endBc) {
                                clearInterval(intervalBc2)
                            }
                        }, timeBc)

                    }
                    if (parent.classList.contains('blue')) {
                        let start = 0
                        let end = 9452
                        let step = 139

                        let interval2 = setInterval(() => {
                            start += step

                            value.textContent = `${start}`
                            if (start === end) {
                                clearInterval(interval2)
                            }
                        }, 30)
                        let startBc = 0
                        let endBc = 280
                        let timeBc = 5

                        let intervalBc2 = setInterval(() => {
                            startBc++
                            parent.style.background = `conic-gradient(#1100ff ${startBc}deg, #9d96ff 0deg)`
                            if (startBc === endBc) {
                                clearInterval(intervalBc2)
                            }
                        }, timeBc)
                    }
                    if (parent.classList.contains('green')) {
                        let start = 0
                        let end = 100
                        let step = 1

                        let interval2 = setInterval(() => {
                            start += step

                            value.textContent = `${start}%`
                            if (start === end) {
                                clearInterval(interval2)
                            }
                        }, 16)
                        let startBc = 0
                        let endBc = 360
                        let timeBc = 1

                        let intervalBc2 = setInterval(() => {
                            startBc++
                            parent.style.background = `conic-gradient(#006715 ${startBc}deg, #a6ffc1 0deg)`
                            if (startBc === endBc) {
                                clearInterval(intervalBc2)
                            }
                        }, timeBc)
                    }

                }
            })
        }
        

      

        function popupPop() {
            let popupLinks = document.querySelectorAll('.popup-link')
            let popupLinkClose = document.querySelectorAll('.popup__close')
            let body = document.querySelector('body')
            let lockPadding = document.querySelectorAll('.lock-padding')

            let unlock = true
            let timeOut = 800


            if (popupLinks.length > 0) {
                popupLinks.forEach((link) => {
                    link.addEventListener('click', (e) => {
                        let popupName = link.getAttribute('href').replace('#', '')
                        let currentName = document.getElementById(popupName)
                        popupOpen(currentName)
                        e.preventDefault()


                    })
                })
            }

            if (popupLinkClose.length > 0) {
                popupLinkClose.forEach((clos) => {
                    clos.addEventListener('click', (e) => {
                        popupClose(clos.closest('.popup'))
                        e.preventDefault()
                    })
                })
            }


            function popupOpen(currentName) {

                if (currentName && unlock) {
                    let popupActive = document.querySelector('.popup.open')

                    if (popupActive) {
                        popupClose(popupActive, false)

                    } else {
                        bodyLock()
                    }

                    currentName.classList.add('open')
                    currentName.addEventListener('click', (e) => {
                        if (!e.target.closest('.popup__content')) {
                            popupClose(e.target.closest('.popup'))
                        }
                    })

                }


            }
            function popupClose(popupActive, double = true) {
                if (unlock) {
                    popupActive.classList.remove('open')
                    if (double) {
                        bodyUnLock()
                    }
                }
            }
            function bodyLock() {
                let lockPaddingValue = window.innerWidth - document.querySelector('.wrapper').offsetWidth + 'px'


                if (lockPadding.length > 0) {
                    lockPadding.forEach((lock) => {
                        lock.computedStyleMap.paddingRight = lockPaddingValue

                    })
                }
                body.style.paddingRight = lockPaddingValue
                body.classList.add('lock')

                unlock = false
                setTimeout(function () {
                    unlock = true
                }, timeOut)

            }
            function bodyUnLock() {
                setTimeout(function () {
                    if (lockPadding.length > 0) {
                        lockPadding.forEach((lock) => {
                            lock.style.paddingRight = '0'

                        })
                    }
                    body.style.paddingRight = '0'
                    body.classList.remove('lock')

                }, timeOut)

                unlock = false
                setTimeout(function () {
                    unlock = true
                }, timeOut)
            }
        }
        popupPop()

        // smoothscroll

        SmoothScroll({
            animationTime: 800,
            stepSize: 75,

            accelerationDelta: 30,
            accelerationMax: 2,

            keyboardSupport: true,
            arrowScroll: 50,


            pulseAlgorithm: true,
            pulseScale: 4,
            pulseNormalize: 1,


            touchpadSupport: true,
        })
    })
}
load()

})();

/******/ })()
;